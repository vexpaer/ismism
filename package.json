{
  "name": "ismism",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/validate-content.mjs && vite build && node scripts/generate-wiki-pages.mjs",
    "preview": "vite preview",
    "check": "npm run build",
    "test:smoke": "node scripts/smoke-test.mjs"
  },
  "dependencies": {
    "gsap": "^3.15.0",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "playwright-core": "^1.62.1",
    "vite": "^8.2.1"
  }
}
